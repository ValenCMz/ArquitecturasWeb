package main.Ejercicio5;

import main.Ejercicio5.Modelo.Direccion;
import main.Ejercicio5.Modelo.Persona;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

public class insert {

    public static void main(String[]args){
     /*
        EntityManagerFactory emf = Persistence.createEntityManagerFactory("Example");
        EntityManager em = emf.createEntityManager();

        em.getTransaction().begin();
        Direccion d = new Direccion("Tandil","25 de febrero 555");
        em.persist(d);

        Persona p1 = new Persona(1,"Valentin",22,d);
        Persona p2 = new Persona(2,"Juan",3,d);
        em.persist(p1);
        em.persist(p2);

        em.getTransaction().commit();
        em.close();
        emf.close();

      */
    }
}
